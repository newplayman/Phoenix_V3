# Phoenix V3 修订记录（2025-12-14）

后续测试链压测与参数迭代见 `REVISION_2025-12-15.md`。

本次修订目标：优先消除“错误价格/错误区间→反复失败交易→烧 Gas/建废池/资金回收异常”的系统性风险，并把关键修复同步进文档与脚本，避免继续踩坑。

## 1. 关键问题回顾（为什么会出事）

1) **tick/价格与 decimals 不一致**  
Uniswap V3 链上 `tick` 对应的是 `rawPrice = token1Raw/token0Raw`，必须结合 decimals 才能还原人类单位价格；此前若把 decimals 当作“已经 baked-in”，就会得到错误的 DEX 价格，进而算出错误的 tick 区间，导致：
- 在无效价格区间开仓（“废池/废仓位”）
- 频繁 rebalance/重复 mint
- 高概率 revert，持续浪费 gas

2) **本地 minOut 估算没处理 decimals**  
当 Quoter 不可用时，本地 `minOut` 估算如果直接用 raw amount 与人类价格相乘，会得到离谱的最小输出，造成“保护失效/或保护过严”，两头都不安全。

3) **配置误填 stable_tokens / 依赖 token0/token1 顺序会直接污染价格**  
Phase-1 代码会把 `stable_tokens` 里的 token 价格强行置为 `1.0`。如果误把“CEX 计价 token（例如 WETH）”加入 `stable_tokens`，就会把 WETH 价格覆盖为 1，直接把 Rebalancer/风控/执行带沟里。更关键的是：**Uniswap 的 token0/token1 是按地址排序的**，不同链上 WETH/USDC 可能出现 token0=WETH 或 token0=USDC，不能用“token1 固定是 WETH”的假设。

## 2. 本次修订做了什么

- 修正链上 `tick -> dexPrice`：按 Uniswap V3 原始定义 + decimals 还原，并将运行时价格语义统一为 **stable-per-cex_price_token**（例如 USD/ETH），不再依赖 `token0/token1` 顺序。  
  - 见 `cmd/bot/main.go:697`
- 修正 Engine 的 tick 计算：新增 `StableIsToken0`，当稳定侧并非 token0 时自动做价格倒数转换，避免算出反向/错误 tick 区间。  
  - 见 `internal/engine/asmm.go:69`
  - 见 `internal/engine/interface.go:13`
- 修正本地 `EstimateMinAmountOut`：按 `fromDecimals/toDecimals` 将 raw amount ↔ 人类单位转换，避免错误估值。  
  - 见 `internal/chain/univ3/router.go:149`
- 引入并强制 `pools[].cex_price_token`：明确“哪一个 token 用 CEX feed 定价”；并强制 `stable_tokens` 必须包含池子的稳定侧（且不得包含 `cex_price_token`）。  
  - 见 `internal/config/config.go:177`
- 配置强校验升级：`token0/token1` 必须符合 Uniswap 地址排序（`token0 < token1`），并校验 `pool.address/position_manager/stable_tokens` 都是合法地址，避免“错地址→revert→烧 gas”。  
  - 见 `internal/config/config.go:174`
- 资金安全兜底：链上执行层禁止把 `target/to/recipient` 设为零地址（或空值），避免“回收资金→打到 0x0000 黑洞”。  
  - 见 `internal/chain/gateway/eth_gateway.go:1`
- **修复“反复建同池多个 LP”的根因**：Uniswap V3 的 `NonfungiblePositionManager` 不支持 `tokenOfOwnerByIndex`（不可按 owner 枚举 tokenId），旧逻辑无法同步已有仓位→`Position` 永远是空→Strategy 每轮都 mint 新 NFT。现在改为：  
  - 配置/持久化 `position_token_id`，用 `positions(tokenId)` 同步 tickLower/tickUpper/liquidity。见 `cmd/bot/main.go:1`、`internal/config/config.go:1`、`internal/storage/store.go:1`  
  - rebalance 在非 `dry_run` 下会 **先撤旧仓（decrease+collect+burn）再 mint 新仓**，避免累计多仓位。见 `cmd/bot/main.go:1`
- 收据等待修复：`waitForReceipt` 不再消费 `receiptCh`（避免与 receipt watcher 抢 channel 导致“等不到回执/漏更新”），改为通过 RPC 轮询 receipt。见 `cmd/bot/main.go:1`
- 修复 LP 区间中心价：ASMM Engine 的 LP tick 区间现在以 `DexPrice` 为中心（避免 `CexPrice` 偏离导致 mint 出“离场/废仓位”，引发反复 rebalance 烧 gas）。见 `internal/engine/asmm.go:1`
- 启用波动率自适应宽度（测试链）：`configs/config.yaml` 增加 `strategy.range` 并移除池级别固定宽度，区间宽度会随 `sigma_daily` 调整但不小于 ±5%。见 `configs/config.yaml:1`
- `position_token_id` 学习优先级调整：仓位同步与 rebalance 现在优先使用本地存储记录的 tokenId（避免 rebalance 后 mint 了新 NFT，但 config 仍是旧 tokenId 导致无法同步仓位）。见 `cmd/bot/main.go:1`
- 修正默认配置：`configs/config.yaml` 增加 `cex_price_token`，并将 `stable_tokens` 设置为稳定侧（例如 TUSD），避免价格被污染。  
  - 见 `configs/config.yaml:35`
- 修复建池脚本 `sqrtPriceX96`：`scripts/tusd_setup.py create-pool` 现在把 CLI 输入的 **WETH/TUSD（人类单位）** 转成 Uniswap 所需的 **rawPrice=token1Raw/token0Raw**（自动处理 token0/token1 地址排序 + decimals）再计算 `sqrtPriceX96`。  
  - 见 `scripts/tusd_setup.py:245`
- 脚本增强：新增 `scripts/tusd_setup.py calc-ticks` 自动按目标价格与宽度计算 tickLower/tickUpper 并对齐 tick spacing，避免手填 tick 造成“废仓位”。  
  - 见 `scripts/tusd_setup.py:279`
- 一键重建测试池：`scripts/rebuild_sepolia_tusd_weth_pool.sh`（部署新 TestUSD→建池→算 tick→可选加流动性→输出配置片段；加仓前会做余额预检，避免烧 gas）。  
  - 见 `scripts/rebuild_sepolia_tusd_weth_pool.sh:1`
  - 新增 `SECRETS_FILE` 支持（避免把私钥写进命令/仓库），并修复脚本内嵌 Python 片段所需 env 未导出导致的 `KeyError`。
  - 增加 `PRIVATE_KEY` 格式校验（必须是 64 位 hex，可选 `0x` 前缀），防止误写/截断导致后续流程失败。
  - 解析更稳健：从输出中提取 `0x...40` 地址以识别 `TUSD2/POOL2`，并支持传入 `TUSD2/POOL2` 复用现有 token/pool 继续流程（避免重复部署）。
  - 配置片段修正：按实际 token0/token1 输出对应 decimals，避免再次引入 tick/价格错误。
- 重建流程说明文档：`POOL_REBUILD_SEPOLIA_TUSD_WETH.md`。  
  - 见 `POOL_REBUILD_SEPOLIA_TUSD_WETH.md:1`
  - 补充 `SECRETS_FILE` 的安全用法，避免把私钥暴露到聊天/命令历史里。
- 文档同步：更新建池/加仓 tick 区间示例，并移除仓库内出现的私钥/数据库口令明文。  
  - 见 `DEPLOY.md:187`、`Phoenix V3：跨域流动性指挥官.md:1257`、`TODO.md:135`
- 处理 solcx 下载被 403 拦截：`scripts/tusd_setup.py` 在默认下载失败时自动切换到 `binaries.soliditylang.org`，`scripts/rebuild_sepolia_tusd_weth_pool.sh` 也会默认导出 `SOLCX_LIST_URL/SOLCX_DOWNLOAD_BASE`。  
  - 见 `scripts/tusd_setup.py:160`
  - 见 `scripts/rebuild_sepolia_tusd_weth_pool.sh:64`
  - 如果使用 `SOLCX_BINARY_PATH` 手动提供 solc：现在会复制到 `~/.solcx/solc-v0.8.20`（不再移动/删除原文件）。

## 3. 你需要怎么迁移（避免继续踩坑）

1) **如果你之前用旧脚本创建过 TUSD/WETH 池**：建议重建新池（旧池的初始化价格如果错了，后续靠 swap 很难/成本极高地“纠正”）。  
2) 更新 `configs/config.yaml`：每个池必须设置 `cex_price_token`，并确认 `stable_tokens` 包含稳定侧（且不得包含 `cex_price_token`）。  
3) 先用 `dry_run=true` 跑一轮 rehearsal，确认日志里的 `dexPrice` 与 CEX 价格在同一量级、且 tick 区间合理，再开启实盘。

## 4. 建议的验收标准（最小可验证闭环）

- Bot 日志中：
  - `[DEX] ... tick=... dexPrice=...` 的 `dexPrice` 与 CEX 价格同量级（例如 3,000~4,000）。
  - `Rebalancer` 生成的 `lower_tick < upper_tick`，且接近当前 tick（不会出现“远离当前价格几个数量级”的区间）。
- `configcheck` 能拦住明显危险配置（例如把 `token1` 填进 `stable_tokens`）。
- 在 testnet 小额跑通：swap（若启用）→ mint → collect/withdraw（cleanup），且回收地址为钱包地址而非零地址。

## 5. 已知外部依赖问题（与代码无关，但会卡住流程）

- `solcx` 下载 solc 被 403/网络策略拦截：已做自动 fallback；若仍失败，需手动下载 solc 0.8.20 并设置 `SOLCX_BINARY_PATH`（详见 `POOL_REBUILD_SEPOLIA_TUSD_WETH.md:1`）。

## 6. Sepolia 实际重建结果（本次执行）

- 新 TestUSD（TUSD2）：`0x88048434d39d1174bad7F543C022a02dB70F62c4`
- 新池（WETH/TUSD2, fee=500）：`0x598D2A92FCE90B49F624F2B1c9d88373CaD767a3`
- 初始 tick 区间：`[-196190, -195180]`（±5% around 3180 TUSD/WETH）
- `configs/config.yaml` 已同步更新为以上 token/pool（并修正 token0/token1 decimals 与初始 amount0/amount1）。
- `position_token_id`（UniV3 NFT tokenId）：`221866`（已写入 `configs/config.yaml`，用于仓位同步与 cleanup）。

## 7. dry-run/真跑验收（防止烧 gas）

- `dry_run=true`（带 gateway 但不发交易）已验收：读取到 `dexPrice≈3179.88`，且不再高频生成 `intent`（修复 LP 中心价后稳定）。
- `dry_run=false` 短时观察已验收：未产生任何 `intent`，未发送交易（不会无故关仓/重建 LP）。
