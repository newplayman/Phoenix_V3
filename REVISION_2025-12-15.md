# Phoenix V3 修订记录（2025-12-15）

本次修订目标：在 **Sepolia 测试链** 上把“波动率自适应 LP 宽度 + 自动识别/关闭旧仓位 + 自动铸造新仓位”的链路跑通，并在高频压力场景下把“盲目反复交易/反复建仓/资金回收异常”这类风险进一步收敛到可控范围。

> 注：你在聊天中暴露过私钥；即便是测试链，也建议视为已泄露，后续请更换专用测试私钥。

## 1. 本次关键结论（先说人话）

- **可以自动判断并接管已有 LP（tokenId）并执行“关闭→回收→销毁→再建仓”**：优先使用 DB 记忆的 tokenId；若 DB/配置都为空，会扫描钱包 `tokenOfOwnerByIndex` 自动接管匹配的头寸，避免“同一交易对反复建多个 LP”。
- **波动率自适应宽度已在测试链开启**，并增加了更适合压测的可观测性日志（`sigma_daily / width_pct / vol_window`）。
- **Sepolia 上已跑通含换币的完整闭环**：`drain(保留部分流动性) → swap → finalize close → mint`，解决“测试网自己是唯一 LP、先全撤导致 swap 直接 revert”的死锁。
- 压测中暴露两个典型坑（已修）：
  - **Approve 未及时上链导致 mint 预执行 revert（STF）/ approve 等待超时** → `EnsureAllowance` 增强重试与 gas bump（更稳定）。
  - **position_token_id 指向已 burn 的 tokenId 会导致“反复尝试关闭→直接失败退出”** → 现在把“tokenId 不存在/已 burn”视为已关闭，自动清理本地/DB 的 tokenId 并继续建仓。
- 仍然存在一个“设计层面”的矛盾：**压测参数如果过于敏感，会出现“刚 mint 完又立刻 close”** 的 churn（极烧 gas）。本次加了可配置的 **rebalance cooldown** 来兜底。

### 当前测试链状态（便于你对照）

- Sepolia `tusd-weth-005` 当前有效头寸（wallet `0x39BFa37b4A8A7A20D0F69fd0a388e3EAe739c217`）：
  - `tokenId=221907`
  - `tickLower=-195560` `tickUpper=-195450`
  - 价格区间（TUSD per WETH）：约 `3216.34 ~ 3251.91`
  - 池子当前 `tick=-195490`，链上 `dexPrice≈3238.93`，liquidity=`3515194078760787`

## 2. 本次改动概览（代码/配置）

### 2.1 `cmd/bot/main.go`

- 新增 `-offline-feed`：只模拟 CEX 价格流（制造波动/突变），但 **DEX watcher + 发交易仍走真实 Sepolia**。
- 新增 `-offline-feed-*` 参数：可调步进波动与 jump 概率/幅度，方便快速压测。
- 增加策略周期日志：即使不产生 intent，也会周期性打印 `sigma_daily / width_pct / position ticks / current tick`，便于观察“宽度自适应”是否生效。
- **PositionGuard 覆盖范围扩大**：从“mint 阶段”扩大到“整个 rebalance 执行（包含 close 旧仓位）”，避免 close 期间策略继续生成重叠 intent。
- `positions(tokenId)` 获取失败（常见原因：tokenId 已 burn）时，不再阻塞流程：记录为“已关闭”，并允许后续清理 tokenId、继续建仓。
- 新增可配置 **rebalance cooldown**：在成功 mint 后记录时间，`strategy.rebalance.min_interval` 时间内跳过策略评估，降低“刚建仓就立刻平仓”的 gas 灾难。
- 新增“无 tokenId 时自动接管”逻辑：当 `pools[].position_token_id` 和 DB 都为空时，扫描钱包 UniV3 NFT 选择匹配的最佳头寸作为当前仓位。
- 让 LP 真实变窄：把引擎内写死的 “最小 0.5%” clamp 改为使用 `min_width_pct/max_width_pct` 作为边界，并把策略 profile 的 `engine_risk_factor` 真正传入引擎。

### 2.2 `internal/config/config.go`

- 新增配置段：`strategy.rebalance.min_interval`（duration 字符串，如 `30s`），并在配置校验阶段校验格式。
- 新增（测试链换币闭环）：`chains[].swap_helper_address`、`strategy.rebalance.keep_liquidity_pct_for_swaps`

### 2.3 `configs/config.yaml`

- 打开并强化压测用波动率宽度（`strategy.range.*`），并把参数调到“更窄 + 更敏感 + 不易直接 clamp 到 max”：
  - `min_width_pct: 0.003`
  - `max_width_pct: 0.02`
  - `vol_k: 0.02`（配合测试链合成 feed 的 `sigma_daily` 数值范围，避免长期卡在 max）
  - `vol_window: "3m"`
  - `profiles.normal.min_spread_ticks_multiplier: 0.1`
- 新增：`strategy.rebalance.min_interval: "30s"`

## 3. Sepolia 压测记录（关键片段）

### 3.1 关闭旧仓位 + 新建仓位成功

来自 `/tmp/phoenix_stress_live6.log`：

- 关闭 `tokenId=221879`：
  - `WITHDRAW_221879` tx `0xd666acb23c58c8ffe8cbcd61ae8b5883b7c1cfcca1da56033a4725037bbb4bb2`
  - `COLLECT_221879` tx `0xcb8ba30cafab6772c2b668229506248bf21a6fc668b650527f542e94a8781cd5`
  - `BURN_221879` tx `0x310ecd3afaf131fbdbecc02a12dccb4e8ab2dda6b3bf8031211e780a23f95602`
- mint 新仓位 `tokenId=221880`：
  - mint tx `0x7ca706287c895d2cf29d27f992a80a67ab760763d09de54c12e4d6587fd6933f`

### 3.2 “tokenId 已失效”恢复能力

- 修复前（`/tmp/phoenix_stress_live7.log`）：`position_token_id=221880` 已不存在时，会反复报错并直接退出执行。
- 修复后（`/tmp/phoenix_stress_live8.log`）：识别并继续：
  - `position tokenId=221880 not found (already closed?): execution reverted: Invalid token ID`
  - 随后成功 mint 新仓位 `tokenId=221888`（tx `0x69cfa636bb9e9718e58b4eb6be3255495fa0f7017f5d31c6ce1ec3e0bbb4d1be`）

## 4. 运行方式（测试链建议）

1) 准备密钥（建议只用测试链专用）
- `source /root/.phoenix_secrets`
- `export BOT_PRIVATE_KEY="$PRIVATE_KEY"`

2) 启动 Sepolia 压测（模拟 CEX 波动、真实链上建仓）
- `./bot -config configs/config.yaml -no-api -no-monitor -offline-feed`

3) 调强/调弱 CEX 波动（更快触发宽度变化/再平衡）
- `./bot ... -offline-feed-step-std 0.002 -offline-feed-jump-prob 0.08 -offline-feed-jump-min 0.005 -offline-feed-jump-max 0.02`

## 5. 仍待解决/建议下一步

- **更换/增强 RPC**：公共 RPC（例如 `ethereum-sepolia.publicnode.com`）在高频压测时可能出现 `context deadline exceeded`，导致“receipt 拉取失败→误判状态→重复发交易/重复 approve”。建议优先换成更稳定的 RPC（Infura/Alchemy/Tenderly）或做多 RPC fallback。
- 建议把“压测参数”和“生产参数”完全隔离：压测追求频繁触发，生产需要更强的冷却/阈值/风控，否则年化 100% 的目标会被 gas 与坏单吞噬。

## 6. 追加：后续连续压测（2025-12-15）

来自 `/tmp/phoenix_stress_followup_1.log` 与 `logs/sepolia_stress.log`：

- **cooldown 生效**：mint 后 30s 内会打印 `cooldown active ... skip`，不会立刻把新仓位关掉（避免 churn）。
- **允许失败 tokenId 自愈**：若 `position_token_id` 指向已 burn 的 tokenId，会记录 “not found (already closed?)” 并继续 mint 新仓位。
- **连续多次 mint 成功**（示例 tokenId）：`221890`、`221891`、`221892`（均在日志中可查）。

## 7. 追加：SwapHelper 换币闭环压测（2025-12-15）

来自 `/tmp/phoenix_sepolia_volstress_2.log`：

- `width_pct` 已可落到配置的下限（示例：`sigma_daily=0.5485 width_pct=0.0020`），触发“更窄区间”再平衡。
- 执行顺序验证（关键 tx 示例）：
  - drain（保留 80% 流动性）：`DRAIN_221905` tx `0x09815db074c8a132c988566a3ccb21b5f5bb1d2fd08fe17f19cf865b118924f0`
  - swap（TUSD2→WETH，SwapHelper）：tx `0x5df5a4ed7ab1aa05d861b283b8e930b2eb9faee6545882afe9d9db807650e9ca`
  - finalize close：`WITHDRAW_221905` tx `0x2cecda2c75df02d58ba62edfedd08c7e120ed76762045e9aa9a496d100de45b0` → `COLLECT_221905` tx `0x9a044a111a10130cd6049759492ad7f69802e4a013aa508f30e81cd17ab8e4b0` → `BURN_221905` tx `0xffd10a119c89f86ed30178ee1d21406eb00e0b10db491eaa09edcab5b806c210`
  - swap 后 DEX tick 发生变化（示例：`-195563 → -195509`，dexPrice `3215.37 → 3232.78`），可用于观察区间自适应与再平衡速度。
